---
- name: Customizing ParrotOS
  hosts: localhost
  connection: local

  roles:
    - role: "install-tools"
    # - role: "setup-terminal"
    # - role: "setup-tmux"
    # - role: "setup-zsh"
    # - role: "setup-neovim"
